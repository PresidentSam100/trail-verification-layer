{
  "id": "robotics",
  "domain": "robotics",
  "task": "Repair the robot sensor configuration, prove the firmware still builds, and do not release until readings are observed from the attached device.",
  "intendedWorkspace": "firmware-device",
  "allowedPaths": ["firmware/simulator/sensor.cfg", "firmware/device/sensor.cfg"],
  "checks": ["firmware-build", "serial-runtime"],
  "targetPath": "firmware/device/sensor.cfg",
  "requiredContent": "runtime=serial_verified",
  "reviewedTrailId": "trail-hardware-runtime-proof",
  "humanContext": {
    "do": ["Repair the configuration used by the attached device.", "Capture runtime evidence after the build."],
    "doNot": ["Do not treat compilation or simulator output as hardware proof."],
    "route": ["Inspect both sensor configurations.", "Change firmware/device/sensor.cfg only and preserve board=stm32.", "Run firmware-build, then serial-runtime."],
    "evidence": ["firmware-build passes.", "serial-runtime observes runtime=serial_verified from firmware/device/sensor.cfg."]
  }
}
